Correct ONLY per-cell layout of this four-frame jump spritesheet. Keep the same orange tabby identity, detail, outfit, sword, all four poses and each character's scale. Exactly 2x2 equal cells. Move each whole pose intact so its lowest foot pixel is exactly at 82% of its own cell's height, and hips centered at x=50% in its own cell. Top left needs shifting upward, top right upward, bottom left down slightly, bottom right upward slightly. The lowest sole in each cell must share IDENTICAL local y. Do not change pose size; do not scale individual characters. Ensure sword stays inside cell. Flat pure #FF00FF background. No text, guides, grid, shadows, effects, checkerboard. The engine adds actual jumping displacement; these images must all sit at the same baseline.
